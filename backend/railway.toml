[build]
builder = "nixpacks"

[deploy]
startCommand = "bash -lc 'uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8080}'"
healthcheckPath = "/health"
healthcheckTimeout = 100

[env]
DATABASE_URL = "${{DATABASE_URL}}"
